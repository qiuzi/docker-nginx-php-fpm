{
    "name": "anpanel",
    "description": "Template for deploying php and ngninx to Heroku",
    "repository": "",
    "stack": "container",
    "success_url": "/",
    "env": {
        "CONFIG_FILE": {
          "description": "This value is the base64 string of your preconfigured .config content",
          "value": "",
          "required": false
        },
        "DATABASE": {
          "description": "Whether to reset the database (consider carefully)",
          "value": "no",
          "required": true
        },
         "ADMINEMAIL": {
          "description": "Top administrator email",
          "value": "admin@panel.org",
          "required": false
        },
         "ADMINPASSWD": {
          "description": "Top administrator passwd",
          "value": "admin",
          "required": false
        }
  }
}
